-- =============================================================================
-- Migration: add `business_channel` column to `users` table
-- Date:      2026-06-12
-- Feature:   商务账号标记
-- =============================================================================
--
-- 背景:
--   用户管理页支持标记 / 变更 / 移除"商务账号"，需要存储归属渠道字符串.
--
-- 语义:
--   - 空字符串 ''  = 普通用户
--   - 非空字符串    = 商务账号，值即归属渠道
--
-- 影响:
--   - 新增一列 business_channel VARCHAR(255), 默认 ''.
--   - 现有用户该列均为 ''，等同未标记，业务无影响.
--
-- 兼容性:
--   - GORM AutoMigrate 在 SQLite / MySQL 上启动自动加列;
--   - PG 生产请手动执行下面 ALTER TABLE.
--
-- 回滚: 见文件末尾.
-- =============================================================================


-- -----------------------------------------------------------------------------
-- PostgreSQL (>= 9.6)
-- -----------------------------------------------------------------------------

ALTER TABLE users
    ADD COLUMN IF NOT EXISTS business_channel VARCHAR(255) NOT NULL DEFAULT '';


-- -----------------------------------------------------------------------------
-- MySQL (>= 5.7.8) — 仅在同时部署 MySQL 时执行
-- -----------------------------------------------------------------------------
-- ALTER TABLE users
--     ADD COLUMN business_channel VARCHAR(255) NOT NULL DEFAULT '';


-- =============================================================================
-- 回滚脚本 (出问题时执行)
-- =============================================================================
--
-- PostgreSQL / MySQL:
--   ALTER TABLE users DROP COLUMN business_channel;
